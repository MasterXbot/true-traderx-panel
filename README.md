# True TraderX · Panel

Panel web de True TraderX (bot de opciones). Solo contiene la interfaz: el bot, la estrategia y la base de datos viven en un repositorio privado.

La llave de `config.js` es la **publishable key** de Supabase: es pública por diseño. La seguridad la dan Row Level Security y las políticas de cada tabla.
